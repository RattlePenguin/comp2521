# Introduction
Hash tables allow us to store key-value mappings and search for them in $O(1)$ time.
We hash a given key to ensure it fits within our hash table.

# Reminders
- Make sure to get all your lab marking done soon!

# Topics
- Hash Tables
    - Hashing, Lookup, Insertion, Deletion
    - Linear Probing
    - Separate Chaining
    - Double Hashing

## Hash Tables
Hash tables are an extremely important data structure!
If order does not matter, storing items in a hash table often optimises algorithms by factors of $n$.

The hash function is what allows us to input any given key and still store it properly.
For example

### Linear Probing
### Separate Chaining
### Double Hashing
