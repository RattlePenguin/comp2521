# printList.c
### printList has been implemented recursively, can you try creating the linked list recursively too?
### printList prints an additional space and no newline at its last member, how can you fix this?
