# printList.c
### Printing has been implemented recursively, can you try *creating* the linked list recursively too?
### printList prints an additional space and no newline at its last member, how can you fix this?

# reverseList.c
### Draw out each step of the reverseList function.
### What would happen if the linked list had a cycle?
